platform: linux
image_resource:
  type: docker-image
  source:
    repository: cfbuildpacks/ci
inputs:
  - name: input
outputs:
  - name: output
run:
  path: bash
  args:
    - -c
    - gunzip input/*.tar.gz && xz input/*.tar && mv input/*.tar.xz output/


